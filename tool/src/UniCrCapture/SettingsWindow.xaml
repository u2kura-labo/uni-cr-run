<Window x:Class="UniCrCapture.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Conflict Record Capture の設定" Width="520" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Background="{StaticResource Page}" Foreground="{StaticResource Text}"
        FontFamily="{StaticResource UiFont}" FontSize="13">
  <StackPanel Margin="22">
    <DockPanel>
      <!-- 入れ替えたかどうかが分かるように、バージョンをここに出す -->
      <TextBlock x:Name="VersionText" DockPanel.Dock="Right" VerticalAlignment="Center"
                 Foreground="{StaticResource Text2}" FontSize="11"/>
      <TextBlock Text="SETTINGS" Foreground="{StaticResource Accent}" FontWeight="Bold" FontSize="11"/>
    </DockPanel>
    <TextBlock Text="Conflict Record Capture の設定" FontSize="18" FontWeight="Bold" Margin="0,0,0,6"/>
    <TextBlock x:Name="Intro" Foreground="{StaticResource Text2}" TextWrapping="Wrap" Margin="0,0,0,14"
               Text="リザルト画面でボタン（またはホットキー）を押すと、FF14 の画面を撮って戦績を読み取り、ファイルに追記します。"/>

    <!-- 使い方。どのファイルをビューアに入れるのかが分からない、という声への対処 -->
    <Border Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="1"
            CornerRadius="8" Padding="12,10" Margin="0,0,0,18">
      <StackPanel>
        <TextBlock Text="使い方" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <TextBlock Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,3"
                   Text="1.  FF14 を「ボーダーレスウィンドウ」か「ウィンドウモード」にする"/>
        <TextBlock Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,3"
                   Text="2.  リザルト画面が出きってから、クリスタルのボタン（またはホットキー）を押す"/>
        <TextBlock Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,3"
                   Text="3.  「保存しました」と出れば成功。読めなかったときは「3_読み取りログ」に手がかりが残る"/>
        <TextBlock Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap"
                   Text="4.  「1_戦績_ビューアに入れる」の中の matches-日付.jsonl を、ビューアにドロップする"/>
        <TextBlock Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0"
                   Text="※ ビューアに入れるのは 1 番のフォルダだけです。2 番（画像）と 3 番（ログ）は入れません。"/>
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
          <Button Content="保存フォルダを開く" Style="{StaticResource DarkButton}" Click="OnOpenFolder"/>
          <Button Content="ビューアを開く（Web）" Style="{StaticResource DarkButton}" Margin="8,0,0,0" Click="OnOpenViewer"/>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- 動いているか止めているか。ここから終了もできる -->
    <Border x:Name="StatusBox" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}"
            BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,18">
      <DockPanel>
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
          <Button x:Name="PauseToggle" Style="{StaticResource DarkButton}" Click="OnTogglePause"/>
          <Button Content="終了する" Style="{StaticResource DarkButton}" Margin="8,0,0,0" Click="OnQuit"/>
        </StackPanel>
        <StackPanel VerticalAlignment="Center">
          <TextBlock x:Name="StatusTitle" FontWeight="SemiBold"/>
          <TextBlock x:Name="StatusNote" Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap"/>
        </StackPanel>
      </DockPanel>
    </Border>

    <TextBlock Text="自分のキャラクター名" FontWeight="SemiBold"/>
    <TextBlock Text="リザルト画面の表から自分の行を見つけるのに使います。" Foreground="{StaticResource Text2}" FontSize="12"/>
    <TextBox x:Name="SelfName" Margin="0,4,0,14"/>

    <TextBlock Text="保存フォルダ" FontWeight="SemiBold"/>
    <TextBlock Text="この下に 3 つのフォルダを作ります。ビューアに入れるのは「1_戦績_ビューアに入れる」の中の matches-日付.jsonl です。" Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap"/>
    <DockPanel Margin="0,4,0,14">
      <Button DockPanel.Dock="Right" Content="選ぶ…" Style="{StaticResource DarkButton}" Margin="8,0,0,0" Click="OnBrowse"/>
      <TextBox x:Name="SaveFolder"/>
    </DockPanel>

    <TextBlock Text="ホットキー" FontWeight="SemiBold"/>
    <TextBlock Text="ボタンの代わりに押して撮れます（例：Ctrl+Shift+F12、Alt+F11）。" Foreground="{StaticResource Text2}" FontSize="12"/>
    <TextBox x:Name="HotkeyText" Margin="0,4,0,14"/>

    <TextBlock Foreground="{StaticResource Text2}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,14"
               Text="個人で使うためのツールです。他のプレイヤーの名前が入ったデータや、その画面のスクリーンショットを共有しないでください。作成したファイルには、あなたのキャラクター名が入ります。"/>

    <CheckBox x:Name="KeepImages" Foreground="{StaticResource Text}" Margin="0,0,0,18"
              Content="撮った画像も保存する（読み取りを直したあとに読み直せます）"/>

    <TextBlock x:Name="Error" Foreground="#FF8A8A" TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed"/>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="キャンセル" Style="{StaticResource DarkButton}" Margin="0,0,8,0" IsCancel="True"/>
      <Button Content="保存" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="OnSave"/>
    </StackPanel>
  </StackPanel>
</Window>
