<Application x:Class="UniCrCapture.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <SolidColorBrush x:Key="Page" Color="#070B14"/>
    <SolidColorBrush x:Key="Surface" Color="#0F1623"/>
    <SolidColorBrush x:Key="Surface2" Color="#172033"/>
    <SolidColorBrush x:Key="Text" Color="#EEF3FB"/>
    <SolidColorBrush x:Key="Text2" Color="#A3B1C6"/>
    <SolidColorBrush x:Key="Border" Color="#33475F"/>
    <SolidColorBrush x:Key="Accent" Color="#5CC8FF"/>
    <SolidColorBrush x:Key="Good" Color="#0CA30C"/>
    <SolidColorBrush x:Key="Critical" Color="#D03B3B"/>
    <SolidColorBrush x:Key="Warning" Color="#FAB219"/>
    <FontFamily x:Key="UiFont">Yu Gothic UI, Segoe UI</FontFamily>

    <LinearGradientBrush x:Key="Crystal" StartPoint="0.5,0" EndPoint="0.5,1">
      <GradientStop Offset="0" Color="#8FE3FF"/>
      <GradientStop Offset="0.55" Color="#3987E5"/>
      <GradientStop Offset="1" Color="#D95926"/>
    </LinearGradientBrush>

    <Style x:Key="DarkButton" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="Background" Value="{StaticResource Surface2}"/>
      <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
      <Setter Property="Padding" Value="14,6"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DarkButton}">
      <Setter Property="Background" Value="{StaticResource Accent}"/>
      <Setter Property="Foreground" Value="#04121C"/>
      <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource Surface2}"/>
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
      <Setter Property="CaretBrush" Value="{StaticResource Text}"/>
      <Setter Property="Padding" Value="6,4"/>
    </Style>
  </Application.Resources>
</Application>
